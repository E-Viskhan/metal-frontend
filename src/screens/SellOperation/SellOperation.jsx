import { Box } from "native-base";
import { MyBackButton } from "../../components";

export const SellOperation = () => {

    return (
        <Box bg='white' flex={1}>
            <MyBackButton>Вернуться на главную</MyBackButton>
        </Box>
    );
};